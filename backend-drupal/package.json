{
  "name": "gatsby-drupal-adventure",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "build": "npx lerna run build",
    "build:dev": "npx lerna run build:dev",
    "refresh": "npx lerna run refresh",
    "start": "npx lerna run --parallel start",
    "postinstall": "npx lerna run postinstall"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
